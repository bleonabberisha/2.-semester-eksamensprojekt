<div class="hero">
  <div class="grid1-tekst">
    <div class="container">
      <div class="typed-out">
        <h1>Sustainable slowfashion</h1>
      </div>
    </div>

    <!-- Shop Knap -->
    <div class="button-container">
      <a href="categories.astro" class="shop-button">SHOP NOW</a>
    </div>
  </div>
  <img src="./gallery/hero.webp" alt="heroBillede" class="hero-image" />
</div>

<style>
  .hero {
    position: relative;
    width: 100%;
    overflow: hidden;
  }

  .hero-image {
    display: block;
    width: 100%;
    height: auto;
  }

  .grid1-tekst {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: white;
  }

  .typed-out {
    overflow: hidden;
    white-space: nowrap;
    animation:
      typing 3s steps(20, end) forwards,
      blink 0.8s infinite;
    font-size: 2rem;
    border-right: 0.15em solid white;
    width: 0;
    max-width: 100%;
    margin-bottom: 20px; /* Afstand til knappen */
  }

  .button-container {
    margin-top: 60px;
  }

  .shop-button {
    background-color: #800000;
    color: #fff;
    font-weight: bold;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition:
      transform 0.2s,
      background-color 0.2s;
    text-decoration: none;
  }

  .shop-button:hover {
    background-color: #660000;
    transform: scale(1.05);
  }

  .shop-button:active {
    transform: scale(0.95);
    background-color: #4d0000;
  }

  @keyframes typing {
    from {
      width: 0;
    }
    to {
      width: 100%;
    }
  }

  @keyframes blink {
    from {
      border-color: transparent;
    }
    to {
      border-color: white;
    }
  }
</style>
